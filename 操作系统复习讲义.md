# 操作系统复习讲义

## 资料范围与使用说明

本讲义基于当前分支中的 `Exam` 和 `Mooc` 目录整理，覆盖 `2013.pdf`、`2014.pdf`、`2015-期中.pdf`、`2015.pdf`、`2016.pdf`、`2019.pdf`、`2020.pdf`、`2021.pdf`、`2022.pdf`、`2025.pdf`、`期末分类汇编.pdf`、`操作系统MOOC习题考试合集.pdf`、`选择题分类汇编.pdf`、`quiz.html`。

`Exam\2015.pdf` 是图片型 PDF，无法直接抽取文本；已渲染后人工查看，能确认其题型包括选择题、进程调度、OPT/LRU 页面置换、银行家算法、读者写者 PV、哲学家就餐管程、作业调度。`期末分类汇编.pdf` 和 `选择题分类汇编.pdf` 文件中注明由 Gemini 整理且答案不保证正确，本讲义把它们用于考点归纳，不把可疑答案当作唯一标准。

## 复习优先级

第一优先级：

- 进程状态模型：三状态、五状态、七状态，进程映像组成，唤醒后的状态。
- 调度：FCFS、RR、多级反馈队列、HRRN，能按时间轴写执行序列。
- 页面置换：FIFO、LRU、Clock、OPT，能画页框变化表、算缺页次数、算物理地址。
- 文件索引：i-node 直接块、一级/二级/三级间接块容量与访问次数。
- 磁盘调度：SCAN、电梯调度、C-SCAN，按当前方向写访问序列和移动总量。
- 银行家算法：Need 矩阵、安全序列、Request 试分配。
- PV 和管程：生产者消费者、读者写者、哲学家就餐。

第二优先级：

- 系统调用、陷入机制、内核态/用户态、特权指令。
- I/O 软件层次、DMA、SPOOLing、通道程序。
- 文件系统：目录项、inode、open/create、硬链接、位示图。
- 死锁资源下界、临界区、原语、信号量初值。

第三优先级：

- 操作系统分类：批处理、分时、实时、网络、分布式。
- 多线程模型：用户级线程、内核级线程、混合式，Solaris 多线程实现方式。
- 存储碎片：分页、分段、固定分区、可变分区、段页式。

## 高频选择题速记

### 操作系统概述

- 操作系统管理计算机资源。
- 配置操作系统后的机器称为虚拟计算机。
- 分时系统：多终端、多用户交互；时间片一定时，用户数越多，响应时间越长。
- 实时系统：必须在规定时间内处理外部事件，强调及时性、可靠性、安全性。
- 多道程序设计：在一台处理机上并发运行多个程序，提高 CPU 和外部设备利用率。
- 引入多道程序设计的前提之一是中断功能。
- 并发性：若干事件在同一时间间隔内发生。

### 系统调用与处理器模式

- 系统调用是操作系统向用户程序提供的接口。
- 用户程序的输入输出操作实际由操作系统完成。
- 当 CPU 执行操作系统内核代码时，处理机处于管态/核心态。
- 输入/输出指令必须在管态/核心态下执行。
- 访管指令或陷入机制把用户程序请求转入内核服务。
- `int xxx` 会主动陷入；`div xxx` 可能因异常进入内核；材料中 2021 回忆题给出“不进入内核态”的选项是 `not xx`。

### 进程与线程

- 进程映像组成：程序块、数据块、核心栈、进程控制块 PCB。
- 传统操作系统中，进程是资源分配基本单位；引入线程后，进程仍是资源分配基本单位，线程是调度基本单位。
- 进程从等待态不能直接到运行态，必须先到就绪态。
- 时间片用完：运行态到就绪态。
- 等待事件发生：阻塞/等待态到就绪态。
- 唤醒进程意味着进程变为就绪状态。
- 原语的主要特点是执行不可中断。
- `fork()` 每执行一次，当前所有进程各复制一次；连续 3 次 `fork()` 后总进程数为 8，新产生进程数为 7。
- Solaris 多线程实现方式按历年题为混合式。

### 存储管理

- 分段存储管理不会产生内部碎片；分页存储管理不会产生外部碎片。
- 段式存储的段是“不定长连续”区域。
- 24 位地址中 8 位为段号，则段内位数为 16，每段最大长度为 `2^16`。
- 页式地址转换：`页号 = 逻辑地址 / 页大小`，`页内偏移 = 逻辑地址 % 页大小`，`物理地址 = 页框号 * 页大小 + 页内偏移`。
- 48 位虚地址、32 位物理地址、页长 8KB：页表项数 `2^(48-13)=2^35`，反置页表项数 `2^(32-13)=2^19`。
- 48 位虚地址、32 位物理地址、页长 4KB：正向页表最多 `2^(48-12)=2^36` 项，反置页表 `2^(32-12)=2^20` 项。
- 缺页中断处理完成后，应重新执行被中断的那条指令。
- 虚存可行性基础是局部性原理。
- FIFO 可能产生 Belady 异常；LRU、OPT、Clock 常规题中不作为 Belady 异常算法。

### 设备管理

- DMA：I/O 设备与存储设备交换数据不经过 CPU。
- 通道又称 I/O 处理机，用于主存与 I/O 设备之间的信息传输。
- 通道程序由一系列通道指令组成。
- I/O 软件自顶向下四层：用户空间 I/O 软件、独立于设备的 I/O 软件、设备驱动程序、中断处理程序。
- 设备驱动程序负责把逻辑 I/O 请求转化为物理 I/O 操作的启动和执行。
- CPU 输出速度高于打印机时，可用缓冲技术。
- SPOOLing 实质是把独占设备转化为共享设备；打印结果首先送到磁盘固定区域。

### 文件管理

- Unix 文件索引结构存放在 inode 节点中。
- Unix 通过目录项实现按名存取：目录项保存文件名和 inode 号。
- open 输入参数包含文件名，返回文件描述符。
- 2016 题干确认 create 返回值考查项为文件描述符。
- 文件目录主要作用是按名存取。
- 无结构文件是流式文件。
- 位示图用于磁盘空间管理。
- 文件存取方法：顺序存取、随机存取。
- 硬链接删除原文件名后仍可继续使用，直到最后一个硬链接被删除。

## 简答题模板

### 系统调用过程

答题要点：

1. 用户程序通过系统调用接口提出服务请求。
2. 执行访管/陷入指令，从用户态切换到核心态。
3. 保存现场，根据系统调用号进入系统调用处理程序。
4. 内核检查参数和权限，执行对应服务。
5. 将返回值放入约定位置，恢复现场，返回用户态继续执行。

需要画图时，用“用户程序 -> 系统调用接口 -> 陷入/中断入口 -> 系统调用分派 -> 内核服务例程 -> 返回用户态”表示。

### 三状态、五状态、七状态模型

三状态：

- 就绪、运行、等待/阻塞。
- 就绪到运行：被调度选中。
- 运行到就绪：时间片到或被抢占。
- 运行到等待：等待 I/O 或事件。
- 等待到就绪：等待事件完成。

五状态：

- 新建、就绪、运行、等待/阻塞、终止。
- 新建到就绪：提交/接纳。
- 运行到终止：进程完成或被撤销。

七状态：

- 在五状态基础上增加挂起就绪、挂起等待。
- 就绪/等待可被挂起到对应挂起状态。
- 挂起等待在事件完成后转为挂起就绪。
- 解除挂起后回到就绪或等待。

### 多级反馈队列

模型：

- 多个就绪队列，队列优先级从高到低。
- 高优先级队列时间片短，低优先级队列时间片长。
- 新进程通常进入最高优先级队列。
- 用完时间片未完成则降入下一队列。
- 高优先级队列为空时才调度低优先级队列。

相对 RR 的优势：

- 短作业和交互型作业更容易在高优先级队列快速完成。
- 长作业逐步下沉，减少对短作业响应时间的影响。

缺点和改进：

- 长作业可能长期等待；可用老化/周期性提升优先级缓解饥饿。
- I/O 型进程可能频繁回到高优先级；题目若给出规则，按题目规则执行。

## 计算题模板

### 进程调度时间轴

通用步骤：

1. 按到达时间建立事件点。
2. 每个时间单位写当前可运行进程集合。
3. FCFS：先到先服务，不抢占。
4. RR：按就绪队列轮转，时间片用完且未完成则排到队尾。
5. SRT：选择剩余时间最短，发生更短进程到达时抢占。
6. HRRN：响应比 `R = (等待时间 + 服务时间) / 服务时间 = 1 + 等待时间 / 服务时间`。
7. 多级反馈：先看最高非空队列，时间片用完降级；最低队列常按 FCFS 或题目指定规则。

答题时必须明确：

- 新到达进程在当前时间点是否立即入队。
- 时间片结束与新进程到达同时发生时的入队顺序。
- 题目给出的特殊规则，例如 2016 题说明“就绪队列为空时，正在运行进程不被抢占，继续适用下一段时间片”。

### 页面置换

表格列：访问串、页框 0、页框 1、页框 2、缺页标记。

规则：

- 初始页框为空时，首次装入也算缺页。
- FIFO：淘汰最早调入的页。
- LRU：淘汰最近最久未使用的页。
- OPT：淘汰未来最长时间不再访问的页。
- Clock：命中时引用位置 1；缺页时从指针处扫描，遇到引用位 1 则置 0 并前移，遇到 0 则替换，替换后新页引用位置 1，指针按题目要求移动。

常见数据：

- 2013/2014 页访问串 `4,3,2,1,4,3,5,4,3,2,1,5`，3 个页框；材料中 LRU 缺页 10 次，Clock 缺页 9 次。
- 2015 图片卷页访问串 `2,3,2,1,5,2,4,5,3,2,5,2`，3 个页框；可见手写答案中 OPT 缺页 6 次，LRU 缺页 7 次。

### 银行家算法

步骤：

1. 写出 `Need = Claim - Allocation`。
2. 初始化 `Work = Available`，所有进程 `Finish = false`。
3. 找到满足 `Need <= Work` 的进程，让它完成并释放资源：`Work = Work + Allocation`。
4. 重复直到所有进程完成，得到安全序列；若无法继续，则不安全。
5. Request 题先检查 `Request <= Need` 和 `Request <= Available`。
6. 试分配：`Available -= Request`，`Allocation += Request`，`Need -= Request`。
7. 对试分配后的状态做安全性检查；安全才允许分配。

历年固定数据：

- 2013、2014、2016、2020 都出现过 A/B/C/D 四类资源、P0-P4 的银行家算法数据。
- 2015 图片卷出现 A/B/C 三类资源、P1-P5，要求判断 T0 安全状态，以及 P4、P1 的请求能否分配。

### 磁盘调度

答题步骤：

1. 确认磁道范围。
2. 确认当前位置和刚完成的磁道，用它判断移动方向。
3. 按算法写访问序列。
4. 计算相邻访问点距离之和。

算法：

- SCAN/电梯：沿当前方向服务请求，到端点或无请求后反向。题目若区分 SCAN 和电梯，按题目给出的端点处理规则。
- C-SCAN：只沿一个方向服务，到端点后回到另一端，再继续同方向。
- SSTF：每次选择距离当前磁头最近的请求。

2013 C-SCAN 例：

- 当前 100，刚完成 80，方向向大号磁道。
- 请求：55、58、39、18、90、160、150、38、184。
- 序列：150、160、184、199、0、18、38、39、55、58、90。
- 移动总量：`99 + 199 + 90 = 388`。

### i-node 多级索引

通用公式：

- 每个索引块可存地址数 `N = 盘块大小 / 地址项大小`。
- 直接块容量：`直接地址项数 * 盘块大小`。
- 一级间接容量：`N * 盘块大小`。
- 二级间接容量：`N^2 * 盘块大小`。
- 三级间接容量：`N^3 * 盘块大小`。
- 文件最大长度：`(直接地址项数 + N + N^2 + N^3) * 盘块大小`。

常见数据：

- 2013、2016、2019：12 个直接地址，盘块 1024B，每块可放 256 个盘块地址。
- 2025：块大小 4KB，指针大小 4B，10 个直接块，一级、二级、三级索引各一个。此时 `N=4096/4=1024`。

2025 数据下：

- 直接块最大数据量：`10 * 4KB = 40KB`。
- 所有索引块能表示的数据量：`(1024 + 1024^2 + 1024^3) * 4KB`。
- 换算为 TiB：三级索引部分 `1024^3 * 4KB = 4TiB`，加上一级和二级后约 `4.00TiB`。
- 读取位置访问磁盘次数必须按题面是否已把 inode、索引块、数据块缓存到内存判断；题面没有缓存前提时，不固定成唯一次数。常用答法是：直接块需访问数据块；一级间接需访问一级索引块和数据块；二级间接需访问二级索引块、一级索引块和数据块；三级间接再多一层。

### 作业调度与主存/外设约束

题型特征：

- 主存 100K，可变分区，最先适应，不允许移动。
- 外设如磁带机/打印机静态分配。
- 作业调度常用 HRRN。
- 已进入主存的作业平分 CPU 或按题目给定进程调度策略执行。

解题表格建议：

- 时间点。
- 新到达作业。
- 主存分区占用。
- 外设占用。
- 后备队列中各作业响应比。
- 本时间段运行作业与剩余运行时间。
- 作业装入时间、结束时间、周转时间。

周转时间：

- `周转时间 = 完成时间 - 到达时间`。
- `平均周转时间 = 各作业周转时间之和 / 作业数`。
- `最大周转时间 = max(各作业周转时间)`。

## 并发题模板

### PV 题通用写法

先识别三类信号量：

- 互斥信号量：保护共享缓冲区或计数变量，初值通常为 1。
- 空槽资源信号量：表示还能放多少，初值为缓冲区容量。
- 产品资源信号量：表示已有产品数量，初值为 0。

生产者消费者标准骨架：

```text
semaphore mutex = 1;
semaphore empty = n;
semaphore full = 0;

producer:
while true:
    produce item
    P(empty)
    P(mutex)
    put item
    V(mutex)
    V(full)

consumer:
while true:
    P(full)
    P(mutex)
    take item
    V(mutex)
    V(empty)
    consume item
```

复杂生产链：

- P 生产 A 放入 A 缓冲区：需要 `emptyA`、`fullA`、`mutexA`。
- Q 取两个 A 生产一个 B：连续 `P(fullA)` 两次，再释放两个 `emptyA`；放 B 时用 `emptyB`、`fullB`、`mutexB`。
- R 取三个 B 生产最终产品：连续 `P(fullB)` 三次，再释放三个 `emptyB`。

读者写者写优先要点：

- 读者之间可并发。
- 写者之间互斥。
- 读者和写者互斥。
- 写者优先要求一旦有写者等待，后来的读者不能继续进入。
- 常见变量：`read_count`、`write_count`，以及保护计数的互斥信号量和读写互斥信号量。

哲学家 PV 防死锁常见思路：

- 至多允许 4 个哲学家同时尝试拿筷子。
- 或规定奇偶哲学家拿筷子顺序不同。
- 或使用服务员/互斥控制统一授权。

### Hoare 管程题通用写法

管程答题要素：

- 共享状态变量。
- 条件变量。
- 入口过程。
- 互斥由管程自动保证。
- 条件不满足时 `wait`，状态变化后 `signal`。

哲学家就餐管程骨架：

```text
monitor DiningPhilosophers
    enum {thinking, hungry, eating} state[5]
    condition self[5]

    procedure pickup(i):
        state[i] = hungry
        test(i)
        if state[i] != eating:
            wait(self[i])

    procedure putdown(i):
        state[i] = thinking
        test((i + 4) % 5)
        test((i + 1) % 5)

    procedure test(i):
        if state[i] == hungry
           and state[(i + 4) % 5] != eating
           and state[(i + 1) % 5] != eating:
              state[i] = eating
              signal(self[i])
end monitor
```

读者写者写优先管程要点：

- 记录正在读的读者数、是否正在写、等待写者数。
- 读者进入条件：没有写者正在写，且没有写者等待。
- 写者进入条件：没有读者正在读，且没有写者正在写。
- 写者离开时优先唤醒写者；无写者等待时唤醒所有可读者。

## 按年份看大题重复

- 2013：C-SCAN、i-node、五状态、页表项/反置页表、RR/Feedback、LRU/Clock、银行家、吸烟者 PV、哲学家管程、作业调度。
- 2014：i-node、SCAN/电梯、操作系统三大抽象、RR/Feedback、LRU/Clock、银行家、哲学家 PV、生产者消费者管程、作业调度。
- 2015 期中：七状态、FCFS/RR/SRT/HRRN/Feedback、作业调度。
- 2015 期末图片卷：RR、OPT/LRU、银行家、读者写者 PV、哲学家管程、作业调度。
- 2016：进程映像、电梯调度、三状态、i-node、RR/Feedback、LRU/Clock、银行家、作业调度、吸烟者 PV、哲学家管程。
- 2019：I/O 四层、磁盘调度、fork 进程树、i-node 50M/100M、虚存分页、RR/Feedback、LRU/Clock、作业调度、PV、管程。
- 2020：系统调用、open、操作系统中断驱动、电梯调度、盘块数量、LRU/Clock、进程调度、银行家、生产者消费者 PV、读者写者写优先管程。
- 2021：多级反馈、inode、FIFO/Clock、移臂调度、作业调度、生产者消费者 PV、哲学家管程。
- 2022：Feedback、create、间接盘块、磁盘调度、缺页置换、进程调度、生产链 PV、哲学家管程。
- 2025：电梯算法、进程映像、七状态、索引块和读磁盘次数、三级队列调度、页面替换物理地址、SPOOLing、银行家、PV、写优先读者写者管程。

## 最后两天复习清单

第一遍：背诵概念题

- 系统调用、特权指令、用户态/核心态。
- 进程映像、PCB、进程状态转换。
- I/O 四层、DMA、SPOOLing。
- inode、目录项、open 返回值、create 返回值。

第二遍：手算固定模型

- 进程调度至少练 2013、2016、2025 三类。
- 页面置换至少练 2013/2014、2015、2025 三类。
- 银行家算法至少练 A/B/C/D 资源原题和 2015 A/B/C 资源题。
- 磁盘调度至少练 2013 C-SCAN、2016 电梯。
- i-node 至少练 12 直接块/1024B/256 地址项和 2025 4KB/4B/10 直接块。

第三遍：默写并发模板

- 生产者消费者 PV。
- 吸烟者问题 PV。
- 读者写者写优先 PV 或管程。
- 哲学家就餐管程。

考场检查顺序：

1. 先做选择题中能直接判断的题。
2. 计算题先画表，不要直接心算。
3. 页面置换题每列都标缺页。
4. 银行家算法先写 Need，再写安全序列。
5. PV/管程题先列资源和互斥对象，再写代码。
6. 文件索引题先算每块地址项数 `N`，再判断所在层级。
7. 磁盘调度题先确定方向，再写序列，最后算移动量。
